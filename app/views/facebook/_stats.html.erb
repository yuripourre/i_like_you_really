<div class="page-header">
  <h1>You</h1>  
</div>

<div class="stats">
  <%= image_tag "#{current_user.facebook_image_uri}?width=300&height=300", class: "you" %>

  <ul class="latest nav">
    <li class="happy">
      <%= (cycle_stat_message % ["<span class='count'>#{current_user.happy_friends_count}</span>", "person".pluralize(current_user.happy_friends_count)]).html_safe %>
    </li>
  </ul>
</div>

