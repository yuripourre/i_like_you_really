<% friends.each_with_index do |friend, current| %>
  <%= render partial: "relationship", object: friend, locals: { current: current } %>    
<% end %>

<%= paginate @raw_friends, filter: params[:filter] %>
